export enum PATH {
  ALL_PRODUCTS = '/',
  PRODUCT = '/product-page/:id',
  CART = '/shopping-cart',
}

export enum SortBy {
  byRate = 'byRate',
  byNameAZ = 'byNameAZ',
  byNameZA = 'nameZA',
  lowesPrice = 'lowesPrice',
  highestPrice = 'highestPrice',
}
