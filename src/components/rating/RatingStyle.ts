export const starRatingContainer = {
  display: 'inline-flex',
  alignItems: 'center',
  width: 'fit-content',
};
export const starList = { display: 'flex', flexDirection: 'row', gap: '2px' };
export const starListItem = { display: 'inline', p: 0 };
export const starContainer = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};
